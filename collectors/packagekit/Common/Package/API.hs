-- | This module hosts the Packages API. Maybe it should really just expose
-- types rather than functions, as there is nothing but the web service
-- signature in common between the client and the server.
module MuPromote.Common.Package.API where

{-
import Data.Aeson

import Network.HTTP.Conduit

putPackage = undefined

getPackages = undefined


-- API endpoint autogeneration. Independent parts for Client and Server to
-- relieve the client from Yesod dependencies. Should Yield two libraries that
-- are kept compatible.

serveMethod :: a -> -}
